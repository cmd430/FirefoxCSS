{
  "dependencies": {
    "@vgrid/sass-inline-svg": "^1.0.1",
    "sass": "^1.55.0"
  },
  "scripts": {
    "compile": "node compile.script",
    "compile & install": "node compile+install.script"
  }
}
